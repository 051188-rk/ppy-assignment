/** @type {import('next').NextConfig} */
const nextConfig = {
  // Optional: Disables image optimization (useful for certain hosting setups)
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;